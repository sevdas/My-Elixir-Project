// Task to run mix test
{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "test",
            "type": "shell",
            "command": "mix test",
            "group": "test"
        }
    ]
}

